package com.example.uniquepersnchlg.ui.theme

import androidx.compose.ui.graphics.Color

// Hot pink brand color - the core identity color, used for primary actions and accents
val HotPink = Color(0xFFFF3D7A)
val HotPinkDark = Color(0xFFE0265F)
val HotPinkLight = Color(0xFFFFD1E1)

// Gen-Z accent trio - used sparingly for badges, sparkles, and the splash gradient so the app
// doesn't read as one flat monochrome pink. Kept punchy/saturated, not pastel.
val ElectricPurple = Color(0xFF7C4DFF)
val SunnyYellow = Color(0xFFFFD429)
val MintPop = Color(0xFF00E5A0)
val SkyBlast = Color(0xFF00C2FF)

// Near-black, used for headlines instead of pure black for a slightly softer feel
val InkBlack = Color(0xFF1A1A1A)

val CloudWhite = Color(0xFFFFFFFF)
val OffWhite = Color(0xFFFFF7FA)
val SoftGray = Color(0xFF6B6B70)
val Divider = Color(0xFFF0E3EA)

// Collage background gradient endpoints - kept in the same hot-pink family as the brand
val CollageGradientTop = Color(0xFF3D0F26)
val CollageGradientBottom = Color(0xFFFF3D7A)

// Splash screen gradient - multi-stop, more energetic than the collage background
val SplashGradientStart = Color(0xFF2B0A3D)
val SplashGradientMid = Color(0xFF7C1F6E)
val SplashGradientEnd = Color(0xFFFF3D7A)
